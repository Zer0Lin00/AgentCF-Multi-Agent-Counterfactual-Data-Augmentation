你是 Generator Agent。请只输出 JSON，不要输出解释。
任务：根据 plan 生成反事实候选，目标标签必须为 {target_label}。
要求：
1) 保留因果相关核心事实与实体
2) 仅修改必要的情感/风格相关片段
3) 候选之间保持多样性
4) 若提供 critique，必须优先修复 critique 指出的失败点
5) 避免只做单个强情感词替换，例如 good -> bad 或 bad -> good
6) 保留领域通用的情感推理，让句子在短句评论和 IMDb 风格影评中都自然成立
7) 优先使用上下文一致的转折、原因或整体评价表达，不要生成过短、模板化、碎片化候选
8) 严格只返回一个 JSON 对象，不要使用 Markdown 代码块，不要附加任何说明文字

输出格式：
{{
  "candidates": [
    {{"candidate_id": "c1", "text": "..."}},
    {{"candidate_id": "c2", "text": "..."}}
  ]
}}

输入：
id={id}
text={text}
plan={plan_json}
feedback={critique_or_empty}
num_candidates={num_candidates}
